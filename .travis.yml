sudo: required

language: python

services:
  - docker

before_install:
  - docker run --rm --privileged multiarch/qemu-user-static:register --reset
  - docker build -t eloo/raspberrypi3-homeassistant-git .

script:
  - docker run -d carlad/sinatra /bin/sh -c "hass --version"
